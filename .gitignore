.vscode

# Cloned repo working copies from ingestion - never meant to be tracked
data/

# Python (extraction-service) - never track secrets, venvs, or bytecode caches
.env
extraction-service/env/
__pycache__/
*.pyc

# Pre-Postgres-migration local artifacts (SQLite + Chroma)
chroma_db/
api/knowyourrepo.db
api/knowyourrepo.db-shm
api/knowyourrepo.db-wal
